针对AI功能，做以下的修改：

1. 每次重新点进AI助手模块，都保留之前的聊天记录
2. 聊天时用户的头像怎么在对话框左边？给我改到右边
3. 能不能访问本地后台数据库（比如我想问现在有多少库存，ai助手可不可以直接调用后台告诉我数量，而不是胡编乱造），如果不能，每当用户想访问后台数据时，仅教给用户访问路径，不要胡编结果
4. ai的头像能不能换成一个箱子样式的图案（因为我们做的是仓库管理系统，更贴合主题）